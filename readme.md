command line tool for blocking follower chains and tracking those blocks.

dependencies:
python 3.7 +
requests 2.22
beautifulsoup 4.8

TODOs:
- unblocking
- show info on users (block status, detect change in handle)
